{
  "name": "yangchen-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Yang Chen — embedded systems and real-time data processing portfolio",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs --production",
    "build": "node scripts/build.mjs",
    "check": "node --check public/app.js && node scripts/check.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
